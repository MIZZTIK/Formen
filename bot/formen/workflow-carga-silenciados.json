{
  "id": "formenCargaSilenc",
  "name": "Formen - Cargar silenciados desde el celular",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "formen-celular",
        "options": {}
      },
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        -600,
        0
      ],
      "id": "bb000000-0000-4000-8000-00000000c001",
      "name": "Webhook Evolution",
      "webhookId": "formen-celular-evolution"
    },
    {
      "parameters": {
        "jsCode": "// Evolution manda muchos eventos por el mismo webhook. Aca solo nos interesa uno:\n// un mensaje que SALE del celular del local hacia una persona. Todo lo demas se\n// descarta en silencio devolviendo [], que en n8n corta la ejecucion sin error.\nconst cuerpo = $json.body || $json;\n\n// 1. Solo mensajes nuevos.\nconst evento = String(cuerpo.event || cuerpo.type || '').toLowerCase();\nif (evento && evento.indexOf('messages.upsert') === -1) return [];\n\nconst data = cuerpo.data || cuerpo.message || {};\nconst key = data.key || {};\n\n// 2. Solo salientes. Un mensaje entrante no significa que el humano este atendiendo:\n//    puede ser cualquiera escribiendo al local.\nif (key.fromMe !== true) return [];\n\n// 3. Solo 1-a-1. Los grupos son @g.us y los estados @broadcast: afuera los dos.\nconst jid = String(key.remoteJid || '');\nif (jid.indexOf('@s.whatsapp.net') === -1) return [];\n\n// 4. El numero sale del JID. Ojo: para Argentina WhatsApp a veces entrega el JID con\n//    el 9 y a veces sin el. No lo arreglamos aca a proposito: /api/pausas normaliza a\n//    +549 al guardar, y la comparacion contra el bot es por los ultimos 10 digitos,\n//    asi que engancha igual venga como venga.\nconst dig = jid.split('@')[0].replace(/\\D/g, '');\nif (dig.length < 10) return [];\n\nreturn [{ json: { numero: '+' + dig, ultimos10: dig.slice(-10) } }];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -400,
        0
      ],
      "id": "bb000000-0000-4000-8000-00000000c002",
      "name": "Filtrar saliente"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://www.formen.ar/api/pausas",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ numero: $json.numero, cargadoPor: 'celular del local' }) }}",
        "options": {
          "timeout": 8000
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.3,
      "position": [
        0,
        0
      ],
      "id": "bb000000-0000-4000-8000-00000000c003",
      "name": "Cargar en la lista",
      "credentials": {
        "httpHeaderAuth": {
          "id": "X9LLzlLHYy5ZZm8w",
          "name": "Formen pausas"
        }
      },
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "url": "=https://adminformenar.kommo.com/api/v4/contacts?query={{ $json.ultimos10 }}&with=leads",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "options": {
          "timeout": 8000
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.3,
      "position": [
        -300,
        0
      ],
      "id": "bb000000-0000-4000-8000-00000000c004",
      "name": "Buscar en Kommo",
      "credentials": {
        "httpHeaderAuth": {
          "id": "XFOpX4xAM95NWBU6",
          "name": "Kommo ForMen"
        }
      },
      "onError": "continueRegularOutput",
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "jsCode": "// Solo cargamos numeros que todavia NO existen como lead en Kommo.\n//\n// Es lo que evita que el bot se silencie a si mismo: el bot y el celular del local son\n// la misma linea, asi que las respuestas del bot tambien llegan aca como salientes.\n// Pero a quien el bot le contesta ya tiene lead, y por eso cae en este filtro.\nconst filtrado = $('Filtrar saliente').first().json;\nconst j = $json;\n\n// Si la llamada a Kommo fallo, NO cargamos. Cargar de mas silencia al bot con un\n// cliente real durante 12 horas; no cargar solo pierde una pausa.\nif (j && j.error) return [];\n\nlet tieneLead = false;\ntry {\n  const raw = (j && j.data && typeof j.data === 'string') ? JSON.parse(j.data) : j;\n  const contactos = ((raw && raw._embedded) || {}).contacts || [];\n  tieneLead = contactos.some(c => ((((c || {})._embedded || {}).leads) || []).length > 0);\n} catch (e) {\n  return [];\n}\n\nif (tieneLead) return [];\nreturn [{ json: filtrado }];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -150,
        0
      ],
      "id": "bb000000-0000-4000-8000-00000000c005",
      "name": "Ya existe en Kommo?"
    }
  ],
  "connections": {
    "Webhook Evolution": {
      "main": [
        [
          {
            "node": "Filtrar saliente",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Filtrar saliente": {
      "main": [
        [
          {
            "node": "Buscar en Kommo",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Cargar en la lista": {
      "main": [
        []
      ]
    },
    "Buscar en Kommo": {
      "main": [
        [
          {
            "node": "Ya existe en Kommo?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Ya existe en Kommo?": {
      "main": [
        [
          {
            "node": "Cargar en la lista",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "pinData": {}
}